คาบที่ 7

flask
host 0.0.0.0 ให้คนอื่นเข้าได้เป็นการรันให้คนอื่นเข้าได้
debug 
port คือเข้าไป port เข้าเซิฟเรา

คำสั่ง search ctrl+r หาคำสั่งที่เคยพิมพ์ไป
conda env list 
conda activate mypy38

app.route("/") เป็นการเรียกว่าจะไปใช้เว็บไหนฟังก์ชั่นไหน

คาบที่ 8

web page
web app เอาไว้ให้คนเข้าไปใช้งาน
web service api จุดมุ่งหมายคือให้โปรแกรมต่างๆใช้มันได้

ส่งข้อความระหว่างเว็บ
get ส่งทุกอย่างใน url
post ต้องมีการปั้นขึ้นมาก่อน

เปิด screen เพื่อให้ เว็ปรันได้ตลอดเวลาเมื่อปิดตัว console หรือเวลาเน็ตตัด
screen -S web (สร้าง)
screen -R web (เข้าดู)